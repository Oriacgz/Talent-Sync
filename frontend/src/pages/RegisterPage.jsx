/*
 * WHO WRITES THIS: Frontend developer
 * WHAT THIS DOES: Register form — name, email, password, role selector
 *                 (Student / Recruiter cards). On success navigates to /login.
 * DEPENDS ON: authService, react-router-dom
 */
export default function RegisterPage() {
  return <div>Register Page</div>;
}