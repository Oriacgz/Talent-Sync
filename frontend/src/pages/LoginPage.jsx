/*
 * WHO WRITES THIS: Frontend developer
 * WHAT THIS DOES: Login form — email + password. On success calls setAuth()
 *                 and navigates to role-based dashboard.
 * DEPENDS ON: authService, authStore, react-router-dom
 */
export default function LoginPage() {
  return <div>Login Page</div>;
}