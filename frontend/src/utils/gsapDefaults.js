/*
 * WHO WRITES THIS: Frontend developer
 * WHAT THIS DOES: Shared GSAP animation config objects used across app pages
 * DEPENDS ON: nothing (pure config objects)
 */
export const fadeUp = { from: { opacity: 0, y: 40 }, to: { opacity: 1, y: 0 } };
export const fadeIn  = { from: { opacity: 0 }, to: { opacity: 1 } };