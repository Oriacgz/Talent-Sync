/*
 * WHO WRITES THIS: Frontend developer
 * WHAT THIS DOES: Validates email, password length, GPA range, name length
 * DEPENDS ON: nothing
 */
export const isValidEmail = (email) => true;
export const isValidPassword = (pw) => true;
export const isValidGPA = (gpa) => true;
export const isValidName = (name) => true;